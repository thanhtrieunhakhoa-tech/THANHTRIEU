# raw/ — Nguồn thô (BẤT BIẾN)

Đặt các file nguồn (`.srt`, tài liệu...) vào đây. **Chỉ đọc — KHÔNG BAO GIỜ sửa.**
Đây là nguồn chân lý; mọi khẳng định trong `wiki/` phải truy vết được về đây.
Ảnh và tệp đính kèm để trong `raw/assets/`.

Hiện chưa có nguồn nào. Xem quy trình "Nạp nguồn" trong `CLAUDE.md` (mục 5).
